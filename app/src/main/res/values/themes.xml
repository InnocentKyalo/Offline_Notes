<resources>
    <!-- Base application theme. -->
    <style name="Theme.OfflineNotes" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
      <!--  <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>-->
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.OfflineNotes.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.OfflineNotes.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.OfflineNotes.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomFAB" parent="Theme.MaterialComponents">
        <item name="tint">@color/colorPrimary</item>

    </style>
</resources>